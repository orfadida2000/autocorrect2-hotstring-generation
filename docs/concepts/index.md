# Concepts

The project separates generic AutoHotkey hotstring semantics, typo sampling,
AutoCorrect2 integration, and orchestration. This separation allows either
processing stage to run independently and keeps reporting composable.

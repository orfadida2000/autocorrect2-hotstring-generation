# Workflows

Three public workflows are available:

- typo generation only;
- AutoCorrect2 checking only for manually provided candidates;
- the full composed workflow.

The full workflow reuses the first two rather than duplicating their logic.
